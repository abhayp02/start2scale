import { useState } from "react";
import api from "../../services/api.js";
const fieldMap={"pilot-agreement":["departmentName","startupName","pilotScope","pilotDuration","milestoneSummary"],"ip-data-clause":["departmentName","startupName"]};
export default function GenerateContract(){const [type,setType]=useState("pilot-agreement");const [data,setData]=useState({});const [content,setContent]=useState("");const [error,setError]=useState("");async function submit(e){e.preventDefault();try{const r=await api.post("/templates/generate",{type,data});setContent(r.data.content);setError("");}catch(x){setError(x.response?.data?.message||"Generation failed.");}}return <main className="max-w-2xl p-4"><h1 className="text-xl font-semibold">Generate Contract</h1><form className="mt-3 space-y-3" onSubmit={submit}><label className="block">Template<select className="block w-full border p-2" value={type} onChange={e=>{setType(e.target.value);setData({});}}><option value="pilot-agreement">Pilot agreement</option><option value="ip-data-clause">IP/data clause</option></select></label>{fieldMap[type].map(field=><label className="block" key={field}>{field}<textarea className="block w-full border p-2" value={data[field]||""} onChange={e=>setData({...data,[field]:e.target.value})} required/></label>)}<button className="border px-3 py-2">Generate</button></form>{error&&<p role="alert">{error}</p>}{content&&<pre className="mt-4 whitespace-pre-wrap border p-3">{content}</pre>}</main>}

