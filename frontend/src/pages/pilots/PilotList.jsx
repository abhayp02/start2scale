import{useEffect,useState}from"react";import{Link}from"react-router-dom";import api from"../../services/api.js";import StatusBadge from"../../components/StatusBadge.jsx";
export default function PilotList(){const[items,setItems]=useState([]);const[error,setError]=useState('');useEffect(()=>{api.get('/pilots').then(r=>setItems(r.data.pilots)).catch(e=>setError(e.response?.data?.message||'Failed to load pilots.'))},[]);return <main className="page"><header className="page-head"><div><p className="eyebrow">Outcome delivery</p><h1 className="page-title">Pilot programs</h1><p className="subtitle">Track milestones, KPI evidence, payments and readiness to scale.</p></div></header>{error&&<div className="card text-[#b42318]">{error}</div>}<div className="grid grid-cols-1 gap-4 lg:grid-cols-2">{items.map(p=><article className="card" key={p._id}><div className="flex justify-between"><div><StatusBadge status={p.status}/><h2 className="mt-3 text-lg font-bold text-[#0b1f3a]">{p.challengeId?.requirements?.domain||'Innovation Pilot'}</h2><p className="text-xs text-[#667085]">{p.startupId?.name} · {p.district||'District pending'}</p></div><span className="text-2xl text-[#155eef]">◉</span></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div className="rounded-lg bg-[#f8fafc] p-3"><small className="text-[#667085]">Start date</small><b className="block">{p.startDate?new Date(p.startDate).toLocaleDateString():'Not set'}</b></div><div className="rounded-lg bg-[#f8fafc] p-3"><small className="text-[#667085]">KPIs</small><b className="block">{p.kpis?.length||0} tracked</b></div></div><Link className="btn btn-primary mt-5" to={`/pilots/${p._id}`}>Open pilot dashboard →</Link></article>)}</div>{!error&&!items.length&&<div className="card text-center text-[#667085]">No pilot programs have been created yet.</div>}</main>}
